<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Horfay" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="Theme.AppCompat.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

    </style>
    <style name="Theme.AppCompat.HorfaySplash">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:windowIsTranslucent">true</item>

    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        // Set the splash screen background, animated icon, and animation duration.
        <item name="windowSplashScreenBackground">@color/white</item>

        // Use windowSplashScreenAnimatedIcon to add either a drawable or an
        // animated drawable. One of these is required.
        <item name="windowSplashScreenAnimatedIcon">@drawable/horfay_logo</item>
        <item name="windowSplashScreenAnimationDuration">200</item>  # Required for
        # animated icons

        // Set the theme of the Activity that directly follows your splash screen.
        <item name="postSplashScreenTheme">@style/Theme.Horfay</item>  # Required.
    </style>
    <style name="Theme.Horfay.SplashScreen" parent="Theme.SplashScreen">

        <item name="windowSplashScreenBackground">@color/white</item>


    </style>



    <style name="EditTextTheme" parent="Widget.Design.TextInputLayout">
        <item name="colorControlNormal">#F5F5F5</item>
        <item name="android:background">@color/teal_200</item>
        <item name="colorControlActivated">@color/purple_500</item>
    </style>
    <style name="CustomCheckbox01" parent="@style/Widget.AppCompat.CompoundButton.CheckBox">
        <item name="android:colorControlNormal">#531313</item>
        <item name="android:colorControlActivated">#C2C2C2</item>
        <item name="android:colorControlHighlight">#100F0F</item>
        <item name="android:textSize">15sp</item>
    </style>

    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView">
        <!-- Customize the search icon -->
        <item name="searchIcon">@drawable/search</item>

        <!-- Customize the close icon -->
        <item name="closeIcon">@drawable/ic_baseline_close_24</item>

        <item name="android:icon">@drawable/ic_baseline_close_24</item>

        <!-- Customize the text color -->
        <item name="android:textColor">@color/green</item>

        <!-- Customize the hint text color -->
        <item name="android:textColorHint">@color/yellow</item>

        <!-- Customize the background -->
        <item name="android:background">@drawable/singin_back</item>

        <!-- Change the search icon position to the left -->
        <item name="searchHintIcon">@null</item>
        <item name="android:iconifiedByDefault">false</item>
        <item name="queryBackground">@color/alice_blue</item>
    </style>
</resources>